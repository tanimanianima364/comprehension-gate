node_modules/
coverage/
.comprehension-gate-state/
*.tmp

# Per-developer Claude Code config. Shared project settings (.claude/settings.json,
# committed skills or hooks) stay tracked; only the .local. files are personal.
.claude/*.local.*
.claude/settings.local.json
